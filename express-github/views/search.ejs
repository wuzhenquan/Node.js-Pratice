<h1>Github repositories for <%= search %></h1>
<% if(results.length){ %>
    <ul>
        <% for (var i = 0; i < results.length; i++){ %>
        <li><%= results[i].full_name %> - <%= results[i].owner.login %></li>
        <% } %>
    </ul>
<% }else{ %>
    <p>No results</p>
<% } %>